{
  "name": "digital-wallet-fastapi",
  "private": true,
  "scripts": {
    "start": "uvicorn app:app --host 0.0.0.0 --port 5000",
    "dev": "uvicorn app:app --host 0.0.0.0 --port 5000 --reload",
    "test": "python -m pytest tests/ -v"
  }
}